# Repo-level liberaqt config, picked up by the pytest plugin (rootdir/liberaqt.toml).
#
# No `executable` here on purpose: this repository has no application of its own to point at. The
# file exists to document the section and to give `liberaqt run` a default timeout.
[liberaqt]
timeout = 10.0
